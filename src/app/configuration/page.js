import Config from "@/components/configuration";

export const metadata = {
  title: "Game Configuration",
  description: "Customize the Wordle game settings",
};

export default function ConfigPage() {
  return <Config />;
}
